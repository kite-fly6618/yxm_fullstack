// es6 箭头函数
// const getUserInfo = (user:{name:string, company:string, sex:string} ):string => {
//     // return false;
//      return '${user.name} 性别${user.sex} 就职于${user.company} ';
// }

// console.log(getUserInfo({name: '任康华', sex: '男' , company:'字节' }))